import 'package:flutter/material.dart';

class DisposedPage extends StatelessWidget {
  const DisposedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Disposed View"),
      ),
    );
  }
}
